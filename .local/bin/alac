#!/bin/sh

colorscheme=$(ls "$HOME/.config/alacritty/themes" | fzf --border rounded --height 20%) 

[ "$colorscheme" ] && sed -i "3s@.*@  - ~/.config/alacritty/themes/${colorscheme}@" \
    "$HOME/.config/alacritty/alacritty.yml"
